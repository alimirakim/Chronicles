import React from 'react'


export default function ChoiceForm({ handleChange, setUniqueContent }) {
  return (<>
    {/* TODO A selector that allows choosing an origin thread. */}
    {/* TODO A selector that allows adding threads to list for creating connections. */}
  </>)
}
